# 変更とレビューの進め方 / 변경·리뷰 운영 규칙

2026-09-06부터 모든 변경은 브랜치와 PR을 거친다. 초기 연결 확인을 위해 사용했던 `main` 직접 커밋은 종료했다. 작은 문서 수정도 예외가 아니다.

## 변경 하나의 단위

한 PR에 한 가지 주요 목적을 둔다. 구현 전에 다음을 PR 초안이나 작업 메모에 적는다.

- 해결할 문제와 사용자 요청 또는 관련 로드맵 항목.
- 이번 변경 뒤 플레이어/개발자가 할 수 있게 되는 일.
- 포함 범위와 이번에 건드리지 않을 범위.
- 성공을 판단할 관찰 가능한 조건.

큰 목적을 달성하는 데 필요한 연관 변경은 함께 다룬다. 예를 들어 언어 전환에 필요한 이벤트 구조 변경은 관련 작업이지만, 동시에 AI 상대까지 만드는 것은 별도 목적이다. 회의·진단 요청만으로 구현이나 외부 변경을 시작하지 않는다.

## 브랜치 → PR → 리뷰 → 병합

1. 원격 `main`의 현재 SHA, 열린 관련 PR, 로컬 변경을 확인한다. 다른 작업을 덮어쓰지 않는다.
2. 새 목적에는 새 브랜치를 만든다. 예: `feat/basic-opponent`, `fix/locale-selection`, `docs/repository-governance`. 동일한 미완료 작업만 해당 기존 브랜치에서 이어간다.
3. 필요한 구현·문서·검증을 수행하고 그 브랜치에 커밋한다. GitHub 연결 도구로 작업할 때도 모든 쓰기의 대상 브랜치를 명시한다. Git Data API를 쓸 때 기존 tree를 기반으로 변경 파일만 반영하고, `main` ref를 직접 이동하지 않는다.
4. `main`을 대상으로 PR을 만든다. 진행 중이거나 차단 사항이 있으면 Draft로 둔다. [PR 템플릿](.github/PULL_REQUEST_TEMPLATE.md)을 사용한다.
5. 독립 리뷰를 요청한다. 리뷰어가 본 revision과 목적 적합성 판단, 문제와 심각도, 검증 범위를 PR에 남긴다.
6. 수정 후 영향을 받은 부분을 재검토받는다. 검토 뒤 추가한 문서 전용 커밋도 변경 내역을 명시하되, 결과 기록만 추가한 커밋과 실행 코드 변경을 구분한다.
7. 관련 CI가 통과하고 미해결 차단 지적이 없으며 사용자가 해당 PR의 병합을 승인한 뒤에만 병합한다. 승인은 특정 PR/변경 범위를 대상으로 한다. 승인 뒤 범위가 바뀌면 다시 확인한다. 자동 병합을 임의로 켜지 않는다.

기본 병합 방식은 squash를 제안한다. 이는 운영 기본값이며 아직 GitHub 설정을 변경했다는 뜻은 아니다. 병합 직전 PR head를 다시 읽고, API가 지원하면 기대 head SHA를 전달한다. 병합 결과와 배포 결과는 각각 확인한다.

## 리뷰어의 역할

리뷰어는 “코드가 돌아가는가”와 함께 “이 목적에 이 작업이 필요한가, 충분한가”를 판단한다.

| 확인할 것 | 질문 |
|---|---|
| 목적 적합성 | 사용자가 원한 결과를 실제로 만드는가? 성공 조건에 빠진 작업은 없는가? |
| 범위 | 요청하지 않은 게임 규칙·기획·외부 설정을 바꾸지 않았는가? |
| 정확성 | 상태 전이, 비동기 입력, 재시작 등 관련 경계에서 문제가 없는가? |
| 구조 | 결정론적 규칙/UI, 대국/런 상태, 언어/이벤트의 분리가 유지되는가? |
| 플레이 경험 | 모바일 조작, 한·일·영 표시, 현재 기능과 UI의 약속이 일치하는가? |
| 증거 | 검증 결과가 실제 실행에 근거하는가? 확인하지 못한 부분을 표시했는가? |

심각도는 `blocker`(병합 전 해결), `follow-up`(영향을 설명하고 후속 과제로 합의), `nit`(선택적 개선)로 기록한다. 심각한 결함을 단순히 후속 과제로 낮춰 통과시키지 않는다. 판단은 `changes requested`, `no blocking findings`, `blocked / insufficient evidence` 중 하나로 명시한다. “지적 없음”은 “모든 동작 보증”이 아니다.

리뷰어는 원래 사용자 목적과 실제 diff를 직접 읽고, 구현자의 요약만으로 승인하지 않는다. 구현 파일을 직접 고치는 대신 지적을 구현자에게 반환한다. 검증은 변경 위험에 필요한 만큼만 수행한다.

## AI 독립 리뷰와 GitHub 승인

- 현재 기본 방식은 구현자와 별개의 AI 에이전트/세션에서 독립 검토하고 PR 본문 또는 댓글에 보고서를 남기는 것이다. 별도 에이전트가 불가능하면 자기검토로 대체하지 말고 Draft PR에 독립 리뷰 미완료를 적는다.
- 실제 GitHub 리뷰어 계정/팀을 사용자가 지정했다면 접근 가능한 대상인지 확인한 뒤 리뷰 요청을 보낸다. 임의의 외부 계정을 추가하거나 동료에게 통보하지 않는다.
- 동일 GitHub 계정을 사용하는 별도 AI 검토는 GitHub의 별도 계정 `Approve`가 아니다. 자기 PR 승인으로 위장하지 않으며, 필수 승인 수를 충족했다고 주장하지 않는다.
- 현재 별도 GitHub 리뷰어 계정/팀은 미지정이다. 독립 AI 보고서와 사용자의 병합 승인으로 운영하되, 서버 측 필수 승인 규칙을 설정했다면 실제 조건을 충족할 때까지 대기한다.

## 검증과 CI

`npm ci`, `npm test`, `npm run build`가 기본 실행 경로다(Node 22). 규칙 변경에는 의미 있는 회귀 테스트를 추가한다. 문구 수정만으로 구현 내용을 그대로 복제하는 테스트를 만들지는 않는다.

이 PR은 `CI` 워크플로에 `validate` 작업을 추가한다. `main` 대상 PR에서 테스트·타입 검사·빌드를 실행한다. 문서 PR에도 동일한 check가 생성되도록 경로 필터는 두지 않는다. PR 워크플로는 저장소 읽기 권한만 가지며 배포나 시크릿 사용을 하지 않는다. 기존 Pages 배포는 `main` push 시 실행되므로 병합 후에 수행된다.

언어 전환, 포획/재투입, AI 중 재시작 등 변경과 관련된 UI 확인만 수행한다. 로컬 테스트, CI, 배포, 실제 브라우저, 사용자 실기기 확인은 서로 다른 증거로 적는다.

## 연결 실패와 긴 대기

이 프로젝트에서는 배포/브라우저 확인 호출이 시간 제한을 지정해도 수 시간 반환되지 않은 일이 두 번 있었다. 따라서 세션 마무리 시 이미 확인한 커밋·빌드 결과를 브라우저 확인 때문에 무기한 보류하지 않는다.

- 독립 외부 확인은 한 호출씩 결과를 출력하고 다음 단계로 간다. 여러 외부 확인을 하나의 긴 호출에 숨기지 않는다.
- 도구가 제어를 돌려주는 범위에서 짧은 timeout/yield를 사용한다. 무응답이면 한 번의 제한된 재확인 후 `미확인`으로 인계한다. 도구 바깥의 대기가 강제로 끊긴다고 보장하지 않는다.
- 권한 거절이나 보호 규칙은 우회하지 않는다. 정확한 차단 행동과 필요한 사용자 조치를 적는다. 쓰기 결과가 불명확하면 재시도 전에 원격 상태부터 읽는다.
- 배포 확인 실패를 이유로 코드 변경이나 새 배포를 반복하지 않는다. CI의 확정 실패는 해당 로그를 읽고 고친다.

## GitHub에서 강제하려면

이 문서와 `AGENTS.md`는 행동 규칙이다. 이 PR은 **서버 측 브랜치 보호를 설정하지 않는다**. 현재 보호 설정도 확인하지 못했다. 보호 상태를 추정해서는 안 된다.

소유자가 설정할 때의 목표 구성:

- `main` 대상 branch ruleset을 Active로 설정하고 PR을 통해서만 변경하도록 한다.
- force push와 브랜치 삭제를 차단하며, 불필요한 우회 권한은 두지 않는다.
- 이 PR의 CI가 한 번 실행되어 확인 목록에 나타난 뒤 `validate` 작업에 해당하는 실제 check를 필수로 선택한다. UI 표시 이름은 실행 결과로 확인한다.
- 별도 리뷰어 계정 또는 승인 가능한 봇을 준비하면 필수 승인 1개와 새 커밋 후 이전 승인 무효화를 설정한다. PR 작성자 본인의 승인은 그 요건을 충족하지 못한다.

이 설정을 적용할 권한/도구가 없으면 사용자에게 설정을 요청한다. `CODEOWNERS`나 문서 파일만 추가해 서버 차단이 생겼다고 보고하지 않는다.
