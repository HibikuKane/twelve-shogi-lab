# 다음 세션 인계

기준일: 2026-09-15. 이 파일은 대화 전체를 다시 읽지 않고 이어가기 위한 스냅샷이다. 시작할 때 GitHub의 현재 브랜치/PR/CI 상태를 확인한다.

## 먼저 읽을 것

1. [AGENTS.md](../AGENTS.md): 반드시 지킬 작업 규칙과 저장소 스킬 진입점.
2. [CONTRIBUTING.md](../CONTRIBUTING.md): PR·독립 리뷰·병합·차단 시 처리.
3. [목표](project-goals.md)와 [로드맵](roadmap.md): 무엇을 하려는지와 다음 작업 경계.

## 확인된 main 상태

- 저장소: `HibikuKane/twelve-shogi-lab` (공개).
- 확인한 main: `d47134c` — 운영 규칙·로드맵·인계 정리 PR #1이 병합됨.
- 그 이전 `9621c990998a6bce58e1d2032566682a192d5be5` — 한·일·영 UI와 화면 정리.
- 그 이전 `6c7c087cc6b64316ea5d194b0624368712704193` — 기본 십이장기와 Pages 워크플로.
- main은 아직 로컬 2인 대국만 제공한다. 런/스테이지/장비/융합은 미구현이다. 기본 4대4 배치를 사용한다.
- AI 상대는 아래 브랜치의 PR 제안 상태이며 main에는 없다.
- UI는 개발 질문을 제거하고 대국판·언어 선택·도움말·재시작을 제공한다. 한·일·영 이벤트 번역은 판 상태를 바꾸지 않는다. 저장은 언어 선택만 한다.
- 직전 기능 작업에서 로컬 테스트 15개, 타입 검사, 프로덕션 빌드 통과를 확인했다.

## 배포와 실제 플레이의 증거

- 사용자는 이전 기본판을 폰에서 한 판 플레이했다고 보고했다.
- 최신 다국어 버전은 커밋 반영까지 확인했지만 Pages 배포 성공 및 실제 브라우저 조작 결과를 확인하지 못했다. 기본판의 실기기 확인을 최신 다국어 버전 검증으로 대체하지 않는다.
- 게임 주소: <https://hibikukane.github.io/twelve-shogi-lab/>.
- 배포/브라우저 확인 단계의 복합 도구 호출이 장시간 반환되지 않아 사용자가 중단한 일이 두 번 있었다. 정확히 어느 내부 호출이 막혔는지는 확정하지 못했다. 기존 타임아웃 설정도 그 외부 대기를 끊지 못했다.
- 다음 세션은 브라우저 확인을 필수 선행 조건으로 붙여 무기한 대기하지 않는다. 확인 가능할 때 한 단계씩 짧게 보고하고, 실패 시 미확인 항목으로 남긴다.

## 진행 중 제안: Minimax AI 상대

- 작업 브랜치: `claude/minimax-ai-review-ke15a1`, 기준 커밋 `d47134c`, PR #2. main 병합 안 됨. 독립 리뷰는 통과했고 남은 것은 사용자의 병합 승인뿐이다.
- 목적: 로드맵 2번 "혼자 대국하기". 사람(남쪽) vs AI(북쪽) 한 판을 끝낼 수 있게 한다.
- 구조: `src/ai/`에 탐색(`search.ts`)·평가(`evaluate.ts`)·프로필(`profile.ts`)을 분리했다. 탐색기는 기물 가치나 적 성격을 모른다. 향후 광전사·소환사 같은 적은 프로필 추가만으로 만든다.
- 규칙 계층 변경은 `PositionAnalysis`(공격 칸·사자 위치·트라이 랭크) 추가 export뿐이다. 합법 수·승패 판정은 기존 ruleset을 그대로 쓴다.
- 기본 탐색 깊이 4. 컨테이너 실측으로 최악 분기(30) 국면에서 알파베타 5,887노드 / 557ms, 폰 크기 브라우저 실제 응답 최대 428ms였다. depth 5는 약 5초라 채택하지 않았다.
- 평가 가중치는 전부 임시값이다. 플레이 데이터로 튜닝한 적 없다.
- 독립 리뷰 2회 완료(사용자가 별도 ChatGPT/Codex 세션으로 수행, PR #2 댓글). 1차 `3ff370e`는 changes requested, blocker 1건. 2차 재검토 `bd5d683`은 **no blocking findings**로 blocker 해결을 확인했다. 리뷰어가 직접 테스트 33개·빌드·CI·jsdom 하네스 재현을 수행했다.
  - blocker: 재시작 확인창이 열린 동안 AI 모드로 바꾸고 취소하면, AI 차례인데 예약이 없고 판이 잠긴 채 멈췄다. AI 예약을 `render()` 끝에서 상태로부터 다시 유도하도록 바꿔 같은 종류의 누락을 구조적으로 없앴다.
  - follow-up(미해결, 이번 범위 밖, 2차 재검토에서도 유지): 실제 폰에서 중후반 응답성 확인. 탐색은 메인 스레드 동기 실행이며 깊이 4가 모든 기기에서 짧은 응답을 보장하지 않는다.
- UI 상태 회귀 테스트를 위해 jsdom을 devDependency로 추가했다. 테스트 환경일 뿐 UI 프레임워크나 상태 라이브러리가 아니다.
- 확인한 것: 로컬 테스트 33개, 타입 검사, 프로덕션 빌드, 헤드리스 크로미움(390×844)에서 AI 모드 한 판 종료·언어 전환 중 판 유지·리뷰어 재현 순서 재생.
- 확인하지 못한 것: Pages 배포본과 실기기 확인. 헤드리스 결과를 실기기 확인으로 대체하지 않는다.

## 이번 운영 규칙 변경의 지위

- 작업 브랜치: `docs/repository-governance`, 기준 커밋은 위 `9621c99…`.
- 이 문서와 운영 규칙은 해당 브랜치의 PR 제안이다. main에 직접 넣거나 병합하지 않았다. PR 번호/최신 head/리뷰 상태는 GitHub에서 이 브랜치를 기준으로 확인한다.
- 변경 범위: 운영 문서, 저장소 스킬 2개, PR 템플릿, PR 검증 CI, Pages의 main 전용 실행 조건, 규칙 로딩 경로(`CLAUDE.md`와 `.claude/skills` 심볼릭 링크). 게임 코드 변경은 없다.
- 이번 PR의 독립 리뷰와 최신 검증은 PR 본문 및 연결한 검토 보고서를 우선한다. 이 스냅샷의 과거 테스트 수를 현재 PR 성공으로 해석하지 않는다.
- 2026-09-14에 사용자가 `main protect` ruleset을 Active로 설정했다. PR 필수, force push·브랜치 삭제 차단, `validate` 필수 체크, bypass 목록 없음. 현재 구성과 주의점은 [CONTRIBUTING.md](../CONTRIBUTING.md)의 "GitHub에서 강제하는 것"에 있다.
- 필수 승인 수는 0이며 별도 GitHub 리뷰어 계정은 여전히 미지정이다. AI 독립 리뷰 보고서는 공식 계정 Approve와 다르다.

## 다음 세션의 첫 작업

Minimax AI PR의 독립 리뷰와 사용자 검토를 마친다. 승인 없이 병합하지 않는다. main 직접 푸시 금지 지시는 계속 유효하다.

그 다음 후보는 둘 중 하나이며, 한 번에 같이 하지 않는다.

- AI 밸런싱: 임시 가중치를 실제 플레이 관찰로 조정한다. `console.debug("[ai]", ...)`의 항목별 평가 내역을 근거로 쓴다.
- [로드맵 3번 런의 최소 흐름](roadmap.md). AI를 만들면서 기물 수·체크 규칙·런 시스템을 함께 바꾸지 않는다는 경계는 그대로다.

## 이어갈 때 잊지 않을 기획

- 초반 소수 기물 시작은 플레이어만 약하게 만드는 구상이 아니다. 극초반 적은 플레이어보다 기물 수가 적어야 한다.
- 개발 계획과 실험 질문은 문서에 둔다. 플레이어는 완결된 현재 게임 흐름을 보아야 한다.
- 융합/진화는 핵심 관심사지만 방식은 미정이다. 후보 ID를 개발 순서나 확정 기획으로 해석하지 않는다.
- 런 보유 기물 / 출전 기물 / 전투 포획 손패를 분리하고, 포획한 말의 영구 편입 여부를 임의로 확정하지 않는다.
